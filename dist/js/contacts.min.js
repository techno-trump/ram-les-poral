/******/ (function() { // webpackBootstrap
/******/ 	"use strict";
var __webpack_exports__ = {};
// This entry need to be wrapped in an IIFE because it need to be isolated against other entry modules.
!function() {

;// CONCATENATED MODULE: ./src/js/pages/contacts/sliders.js

function initCardsSlider(selector) {
  log("initCardsSlider::selector: ", selector);
  let slider;
  const mediaMatch = matchMedia("(max-width: 575px)");
  const handleMatch = _ref => {
    let {
      matches
    } = _ref;
    if (matches) {
      if (!slider) {
        slider = init();
      }
    } else if (slider && slider.initialized) {
      slider.destroy();
      slider = null;
    }
  };
  mediaMatch.addListener(handleMatch);
  handleMatch(mediaMatch);
  function init() {
    return new Swiper(selector, {
      observer: true,
      resizeObserver: true,
      slidesPerView: 1,
      grabCursor: true,
      autoplay: {
        delay: 5000
      }
    });
  }
}
function initFiltersSlider() {
  new Swiper("#filters-slider", {
    observer: true,
    resizeObserver: true,
    slidesPerView: "auto",
    grabCursor: true,
    spaceBetween: 30
  });
}
function initSliders() {
  //initCardsSlider("#categories-slider");
  //initCardsSlider("#popular-slider");
  //initFiltersSlider();
}
;// CONCATENATED MODULE: ./src/js/pages/contacts/index.js


window.addEventListener("DOMContentLoaded", onLoaded);
function onLoaded() {
  initSliders();
}
}();
// This entry need to be wrapped in an IIFE because it need to be isolated against other entry modules.
!function() {
// extracted by mini-css-extract-plugin

}();
/******/ })()
;